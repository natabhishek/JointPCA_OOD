postprocessor:
  name: jointpca
  APS_mode: False

  postprocessor_args:

    # Maximum number of ID samples used to fit PCA.
    # Default 45000 for ResNet-50/ViT; ResNet-18 uses 9000 automatically.
    max_train_samples: 45000

  postprocessor_sweep: {}
